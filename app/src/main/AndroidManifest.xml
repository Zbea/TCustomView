<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="view.zbea.com.tcustomview" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar" >
        <activity
            android:name=".MainActPage"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ErrorOrSuccessViewPage">
        </activity>

        <activity android:name=".FlowViewPage">
        </activity>
        <activity android:name=".CircleViewPage">
        </activity>
        <activity android:name=".FloatTopViewPage">
        </activity>

        <activity android:name=".WaterfallViewPage">
        </activity>
        <activity android:name=".ScaleViewPager">
        </activity>
        <activity android:name=".RefreshListPage">
        </activity>

    </application>

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

</manifest>
